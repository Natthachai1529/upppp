<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <template id="report_billing_note_document">
            <t t-call="web.external_layout">
                <t t-set="o" t-value="o.with_context(lang=lang)"/>
                <!--                <t t-set="forced_vat" t-value="o.fiscal_position_id.foreign_vat"/> &lt;!&ndash; So that it appears in the footer of the report instead of the company VAT if it's set &ndash;&gt;-->
                <t t-set="address">
                    <address t-field="o.partner_id"
                             t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": True}'/>
                    <div t-if="o.partner_id.vat" class="mt16">
                        <t t-if="o.company_id.account_fiscal_country_id.vat_label"
                           t-esc="o.company_id.account_fiscal_country_id.vat_label" id="inv_tax_id_label"/>
                        <t t-else="">Tax ID</t>:
                        <span t-field="o.partner_id.vat"/>
                    </div>
                </t>
                <div class="page">
                    <h2>
                        <span t-if="o.state == 'posted'">Billing note</span>
                        <span t-if="o.state == 'draft'">Billing note (Draft)</span>
                        <span t-if="o.number != '/'" t-field="o.number"/>
                    </h2>

                    <div id="informations" class="row mt-4 mb-4">
                        <div class="col-auto col-3 mw-100 mb-2" t-if="o.date" name="billing_date">
                            <strong>Billing Date:</strong>
                            <p class="m-0" t-field="o.date"/>
                        </div>
                        <div class="col-auto col-3 mw-100 mb-2" t-if="o.partner_id.ref" name="customer_code">
                            <strong>Customer Code:</strong>
                            <p class="m-0" t-field="o.partner_id.ref"/>
                        </div>
                        <div class="col-auto col-3 mw-100 mb-2" t-if="o.billing_term_id" name="billing_term">
                            <strong>Billing term:</strong>
                            <p class="m-0" t-field="o.billing_term_id.name"/>
                        </div>
                    </div>
                    <table class="table table-sm o_main_table" name="billing_line_table">
                        <thead>
                            <tr>
                                <th name="th_description" class="text-left">
                                    <span>Invoice No.</span>
                                </th>
                                <th name="th_quantity" class="text-right">
                                    <span>Invoice date</span>
                                </th>
                                <th name="th_invoice_amount"
                                    class="text-right">
                                    <span>Invoice amount</span>
                                </th>
                                <th name="th_price_unit" t-if="display_discount"
                                    t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                    <span>Disc.%</span>
                                </th>
                                <th name="th_invoice_balance" class="text-left">
                                    <span>Invoice balance</span>
                                </th>
                                <th name="th_wht_total" class="text-left">
                                    <span>WHT</span>
                                </th>
                                <th name="th_paid_amount" class="text-right">
                                    <span>Total</span>
                                </th>
                            </tr>
                        </thead>
                        <tbody class="billing_tbody">
                            <t t-set="current_subtotal" t-value="0"/>
                            <t t-set="lines"
                               t-value="o.line_ids.sorted(key=lambda l: (l.date_invoice, l.invoice_id, -l.id), reverse=True)"/>

                            <t t-foreach="lines" t-as="line">
                                <t t-set="current_subtotal" t-value="current_subtotal + line.balance"/>
                                <tr t-att-class="'bg-200 font-weight-bold'">
                                    <td name="account_invoice_line_name">
                                            <span t-field="line.invoice_id.name" t-options="{'widget': 'text'}"/>
                                        </td>
                                        <td name="account_invoice_line_date">
                                            <span t-field="line.date_invoice" t-options="{'widget': 'date'}"/>
                                        </td>
                                        <td class="text-right o_price_total">
                                            <span class="text-nowrap text-right" t-field="line.amount"/>
                                        </td>
                                        <td class="text-right o_price_total">
                                            <span class="text-nowrap text-right" t-field="line.balance"/>
                                        </td>
                                        <td class="text-right o_price_total">
                                            <span class="text-nowrap text-right" t-field="line.wht_total"/>
                                        </td>
                                        <td class="text-right o_price_total">
                                            <span class="text-nowrap text-right" t-field="line.paid_amount"/>
                                        </td>
                                </tr>
                                <tr class="is-subtotal text-right">
                                    <td colspan="99">
                                        <strong class="mr16">Subtotal</strong>
                                        <span
                                            t-esc="current_subtotal"
                                            t-options='{"widget": "monetary", "display_currency": env.company.currency_id}'
                                        />
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    <div t-if="not is_html_empty(o.note)" name="comment">
                        <span t-field="o.note"/>
                    </div>
                </div>
            </t>
        </template>

        <template id="report_billing">
            <t t-call="web.html_container">
                <t t-foreach="docs" t-as="o">
                    <t t-set="lang" t-value="o.partner_id.lang"/>
                    <t t-call="l10n_th_billing_note.report_billing_note_document" t-lang="lang"/>
                </t>
            </t>
        </template>
        <!--We need to create the following empty report template for the action report
            "action_account_original_vendor_bill" to work. The action is merging the
            original vendor bill(s) that were used to create the vendor bill(s) into one PDF. -->
        <template id="report_original_billing_note">
            <t t-call="web.html_container">
                <t t-foreach="docs" t-as="o">
                    <div class="article" t-att-data-oe-model="o and o._name" t-att-data-oe-id="o and o.id"
                         t-att-data-oe-lang="o and o.env.context.get('lang')"></div>
                </t>
            </t>
        </template>
    </data>
</odoo>
